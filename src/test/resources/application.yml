spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create
      hbm2ddl: auto
      dialect: org.hibernate.dialect.H2Dialect