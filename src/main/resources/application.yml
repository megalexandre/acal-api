spring:
  jackson.time-zone: America/Sao_Paulo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/acaldb
    username: root
    password: TrObccrzH11dUR=/ytVjTGwOCiK8!mba
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
      hbm2ddl:  auto
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
        check_nullability: true
  output:
    ansi:
      enabled: always

server:
  port: 8080